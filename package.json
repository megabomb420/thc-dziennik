{
  "name": "thc-dziennik",
  "version": "1.3.4",
  "private": true,
  "scripts": {
    "dev": "vite",
    "test": "playwright test",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "vite": "^5.4.0"
  }
}
